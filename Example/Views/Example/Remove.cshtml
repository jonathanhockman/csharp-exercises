<h1>@ViewBag.title</h1>

<form method="post" >
    @foreach(var cheese in ViewBag.cheeses)
    {
        <input type="checkbox" name="cheeseIds" value=@cheese.Id >
        <label>@cheese.Name</label>
        <br />
    }
    <input type="submit" value="Remove cheese(s)" />
</form>